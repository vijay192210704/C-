#include <iostream>
using namespace std;

int main() {
    int a, b;
    cout << "Enter two numbers: ";
    cin >> a >> b;

    cout << (a == b ? "Numbers are equal" : "Numbers are not equal") << endl;

    return 0;
}
